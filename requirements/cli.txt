# ============================================
# DeepTutor CLI — Full Dependencies
# ============================================
# Install: pip install -r requirements/cli.txt && pip install -e .
# Everything needed to run the CLI with all tools (RAG, code execution, etc.)

# --- Config & templating ---
python-dotenv>=1.0.0
PyYAML>=6.0
jinja2>=3.1.0

# --- LLM ---
openai>=1.30.0
tiktoken>=0.12.0

# --- LLM provider SDKs ---
anthropic>=0.30.0
dashscope>=1.14.0
perplexityai>=0.1.0
oauth-cli-kit>=0.1.1; python_version >= "3.11"
google-generativeai>=0.5.0

# --- HTTP clients ---
aiohttp>=3.9.4
httpx>=0.27.0
requests>=2.32.2

# --- Async helpers ---
nest_asyncio>=1.5.8
tenacity>=8.0.0

# --- Data models & storage ---
pydantic>=2.0.0
pydantic-settings>=2.0.0
aiosqlite>=0.19.0

# --- RAG (LlamaIndex) ---
llama-index>=0.14.12
PyMuPDF>=1.26.0
numpy>=1.24.0,<2.0.0
arxiv>=2.0.0

# --- Web search ---
ddgs>=9.9.1
tavily-python>=0.3.0
jina>=5.0.0
serper>=0.3.0

# --- CLI framework ---
typer>=0.9.0
rich>=13.0.0
prompt_toolkit>=3.0.36

# --- Document parsing ---
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=1.0.0
pypdf>=4.0.0
defusedxml>=0.7.1

# --- DeepSeek V4 features (optional) ---
# pip install pyright  # for LSP diagnostics (Python)
# rust-analyzer is required for Rust diagnostics (install from https://rust-analyzer.github.io/)
# clang is required for C/C++ diagnostics (apt/yum install clang)